- Start server: 
    ```bash
        uvicorn app.main:app --reload
    ```
1. Bootstrapped App
2. Connected to MongoDB
3. Test case Imlementation to get all employees
    - Running pytest command : 
    ```bash
    pytest -v {'path to test file'}
    pytest -v ./tests/employees_test.py
    ```
4. Schema creation
5. Model implementation
6. Controller implementation
7. Routes / APIs